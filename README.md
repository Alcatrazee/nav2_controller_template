# nav2_lqr_controller

This package is a scaffold/template for a nav2 controller plugin (LQR controller).

Files added:
- `include/nav2_lqr_controller/lqr_controller.hpp` — controller skeleton header
- `src/lqr_controller.cpp` — skeleton implementation
- `plugin_description.xml` — pluginlib descriptor

Next steps: implement LQR logic, adapt interfaces to `nav2_core::Controller`, and run `colcon build`.
